# app-demo_todo

Goal: todo application which is used as go example

Requirements:
- install and configure git locally
- clone this repo using git
- install and configure node.js locally
- install tailwindcss:  
  *npm install tailwindcss @tailwindcss/cli*
